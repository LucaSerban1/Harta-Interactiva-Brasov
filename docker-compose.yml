services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: brasovmap
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: secret
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: